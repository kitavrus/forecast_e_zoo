# PRD + ADR: Модуль выгрузки данных (data-export adapter)

Дата: 2026-05-05
Источник: интервью `/spec-interview` (см. план `~/.claude/plans/prd-adr-sunny-cosmos.md`).

---

## 1. Context

Фича на основе [diagram_0_overview.html](../../diagram_0_overview.html), задача **«Выгрузка данных»** в дорожке ERP клиента E-Zoo (Фаза 1 — Сбор данных). На диаграмме рядом — отдельная дорожка «Адаптер источников» (1С/ERP/WMS/OMS → единый формат), и проектируем мы именно её — pull-сторону.

**Зачем сейчас:** без надёжного потока справочников и фактов из ERP E-Zoo не работают ни прогноз, ни калькуляция, ни EDI. Все KPI Replenishment (OSA, OTIF, Stock days) зависят от полноты и свежести данных на входе. Сегодня поток описан только концептуально (см. [diagram_1_data_collection.html](../../diagram_1_data_collection.html), [README_summary.md](../../README_summary.md) §5).

**Что получим:** канонический REST-сервис на Go (`/v1/...`), который ежесуточно вытягивает мастер-данные и движения из ERP E-Zoo через её REST API, материализует в PG18 и предоставляет X-Flow read-only выборки с гарантиями полноты, идемпотентности, версионируемой схемы.

**Стек:** Go 1.26 + Fiber v3 + PostgreSQL 18 + pgx/v5 + go:embed + golang-migrate (без auto-apply) + dockertest. CLAUDE.md в репо отсутствует — раздел 8 как ориентир недоступен; домен реконструирован из [README_summary.md](../../README_summary.md) и набора диаграмм.

---

## 2. Goals / Non-goals

**Goals (MVP):**
- Pull-адаптер для одного источника (ERP E-Zoo, REST/SOAP API).
- Полная сутечная выгрузка справочников и движений из §5 ТЗ.
- Материализация в PG18: snapshot-таблицы с `load_id`.
- REST-контракт `/v1/...` с content-negotiation (NDJSON inline / Parquet через signed URL).
- Severity-движок валидации: critical → fail load, soft → quarantine.
- Retention: 30 дней hot в PG + 365 дней cold в S3 Parquet.
- Observability: метрики Prometheus, 4 класса алертов.

**Non-goals (вне MVP, см. §11):**
- Реальный CDC (логическая репликация / outbox в ERP).
- Multi-source (WMS, OMS, DWH) — интерфейс `SourceReader` оставляем, реализация одна.
- Bi-temporal SCD-2 для **мастер-данных** (для фактов bi-temporal — да).
- Собственный ops UI / business dashboard (только Grafana и admin-API).

---

## 3. Users & use cases

| Роль | Сценарий | Поверхность |
|---|---|---|
| **DevOps/SRE** наша команда | Видит «вчера выгрузка прошла за 47 мин, 0 critical, 12 soft» | Prometheus + Grafana + structured logs + admin-CLI |
| **Data engineer X-Flow** | Вытягивает `/v1/receipts?since=...`, ретраит окно | OpenAPI v1 + admin-API `/admin/loads/{id}/retry` |
| **IT-отдел E-Zoo** | Видит «кто и когда читал нашу ERP, какой объём» | Audit log + read-only dashboard (отсроченно, не MVP) |
| **Бизнес-владелец Replenishment** (категорийный менеджер) | «У меня вчера со всех магазинов не пришли чеки» | Через SRE-эскалацию + бизнес-метрики в Grafana (в MVP — без своего UI) |

---

## 4. Functional requirements

### 4.1. Триггер
- **Гибрид:** внутренний cron (Go, в самом процессе) + admin-API `POST /admin/loads` для manual/retry.
- Окно по умолчанию — раз в сутки, время и TZ настраиваются в config.
- Lock-файл/advisory lock в PG чтобы исключить параллельный запуск.

### 4.2. Жизненный цикл прогона
1. `load` начинается → запись `loads(id, started_at, status='running', source='erp_e_zoo')`.
2. По очереди тянутся группы (см. §5): мастер-данные → факты.
3. На каждую сущность — pull через REST с курсорной пагинацией + checkpoint.
4. **Atomic restart**: при сбое весь `load_id` помечается failed, snapshot-pointer не двигается.
5. По завершении — `loads.status='committed'`, `snapshot_pointer.current_load_id := load_id`.
6. Старые `load_id` уходят на retention sweeper (sec §8).

### 4.3. Валидация (severity)
- **Critical** (load_id → failed):
  - FK violation на отсутствующий master (например, чек ссылается на неизвестную товар/баркод/store).
  - NULL в primary key.
  - Отрицательные остатки / отрицательные количества в чеке.
  - `event_time` в будущем (>now+15min, защита от часов источника).
- **Soft** (строка → `reject_log` с причиной, остальное идёт):
  - NULL в необязательном attribute (бренд, страна).
  - Невалидный формат (даты, EAN-checksum).
  - Out-of-range numeric (<0 в полях, где нет смысла, кроме остатков).
- Правила хранятся в config-файле (`validation_rules.yaml`) и загружаются на старте — без deploy.

### 4.4. PII
- На ингесте чеков `loyalty_id` → `loyalty_hash := SHA256(loyalty_id || tenant_salt)`.
- Telephone/email из чеков **не выгружаются вообще** (drop в маппинге).
- Соль хранится в Vault, ротация — не чаще раза в год (иначе теряется join на исторических данных).

### 4.5. UPSERT-семантика мастер-данных
SCD-2 для мастера отложен (ADR-003), хранится только current state. Чтобы это не превратилось в «тихую перезапись без следов» — фиксированные правила:

- **`product_id` PK** → `INSERT ... ON CONFLICT (product_id) DO UPDATE SET ...`.
- **`created_at`** — НЕ перезаписывается (`ON CONFLICT` его не трогает). Дата первого появления карточки в нашей БД.
- **`updated_at`** — перезаписывается ТОЛЬКО при реальном diff tracked-полей (через `WHERE products.* IS DISTINCT FROM EXCLUDED.*` или `BEFORE UPDATE` триггер). Не дёргается на каждом ежесуточном load.
- **`load_id`** — перезаписывается всегда, даже без diff. Нужен как маркер «строка ещё видна в источнике».
- **Все остальные поля** — перезаписываются значением из EXCLUDED (новой выгрузки).
- **Дубликат `product_id` внутри одного `load`** → **critical** (источник прислал две версии в одной выгрузке = битый экспорт).

#### 4.5.a Журнал изменений мастер-данных (`master_change_log`)
Append-only лог значимых изменений. **Это не SCD-2** — нет point-in-time восстановления; даёт только ответ «что и когда менялось». Список tracked-полей задаётся config-driven (`master_tracked_fields.yaml`), стартовый набор:
- `products`: `name`, `brand`, `manufacturer`, `weight_kg`, `pallet_qty`, `shelf_life_days`, `storage_temp_min`, `storage_temp_max`, `requires_prescription`, `is_dangerous_goods`, `category_path`, `status`
- `product_barcodes`: `product_id` (миграция баркода между карточками), `pack_qty`, `is_primary`, `country_origin`

| Поле | Тип |
|---|---|
| `event_id` | UUID PK |
| `entity` | TEXT (`products` / `product_barcodes`) |
| `entity_pk` | JSONB (например `{"product_id":"123"}` или `{"barcode":"4607..."}`) |
| `field` | TEXT |
| `old_value` | JSONB NULL |
| `new_value` | JSONB |
| `changed_at` | TIMESTAMPTZ NOT NULL |
| `load_id` | UUID FK |

Эндпоинт: `GET /v1/master_change_log?entity=...&since=...&field=...` — для X-Flow/категорийщика, чтобы видеть «что изменилось со вчера». Если позже понадобится полноценный SCD-2 — backfill из этого журнала.

### 4.6. Read-during-write
- X-Flow всегда видит последний committed snapshot (`snapshot_pointer.current_load_id`).
- Текущий запущенный `load` не виден в `/v1/...` ответах.
- Минимум 2 snapshot хранятся одновременно (текущий + предыдущий) для отката.

---

## 5. Data model

### 5.1. Канонические сущности (на основе [README_summary.md](../../README_summary.md) §5)

#### 5.1.1. Товары (`products`)
Базовая карточка товара (Product Master / Item Master в ERP-терминологии; «Номенклатура» в 1С). У одного товара может быть **несколько баркодов** (разные упаковки, перепакованные партии, региональные EAN). Хранятся отдельной таблицей `product_barcodes` с FK на `product_id` для нормализованного поиска и уникальности EAN.

| Поле | Тип | Notes |
|---|---|---|
| `product_id` | TEXT PK | внутренний ID E-Zoo (бывш. `sku_id`) |
| `name` | TEXT | торговое наименование |
| `brand` | TEXT NULL | Бренд (Royal Canin, Whiskas, Pro Plan…). Критично для отчётов «доля бренда в категории», фильтров категорийщика, переговоров с поставщиками, ABC-анализа по бренду. Может отличаться от производителя (бренд PetXX выпускает завод-контрактник). |
| `manufacturer` | TEXT NULL | Завод-производитель / правообладатель торговой марки. Нужен для разруливания одноимённых брендов и для compliance. NULL допустим если ERP не различает бренд и производителя. |
| `weight_kg` | NUMERIC(10,3) | Вес единицы товара (с упаковкой). |
| `length_mm`, `width_mm`, `height_mm` | INTEGER NULL | Габариты единицы. **Без них не работает §4 ТЗ «Лимиты по объёму, паллетам, весу»** в калькуляции заказа. Если нет в ERP — soft, считаем объём как 0 (заказ не лимитируется по объёму, фиксируем риск). |
| `pallet_qty` | INTEGER NULL | Стандартное число consumer-единиц на паллете. На уровне карточки, потому что для большинства товаров укладка одинакова. Если бизнес подтвердит наличие нескольких паллетных конфигураций на один товар — выносим в `product_barcodes` (паллетный ITF-14). |
| `shelf_life_days` | INTEGER NULL | Заявленный срок годности **карточки** в днях (атрибут товара, не партии). Используется в калькуляции заказа (не везти больше, чем продастся до истечения), для распознавания списаний по сроку и для сверки с `expiry_date` партии в DESADV. Источник — поле «Срок годности» в ERP, нормализуем в дни (месяцы×30, годы×365). `NULL` = бессрочно (миска, поводок), не «неизвестно». Срок конкретной партии = `production_date + shelf_life_days` и хранится не здесь, а в движениях (`stock_movement`) при приёмке. **Из критики diagram_review §1.1.** |
| `storage_temp_min` / `storage_temp_max` | NUMERIC(4,1) | °C, **из критики §1.1**. Используется для проверки температурного режима на приёмке и для маршрутизации (рефрижератор vs обычный транспорт). |
| `requires_cold_chain` | BOOLEAN | Производное от `storage_temp_max < 8°C`, но материализуем для индексирования: «все товары холодовой цепи» — частый фильтр для логистики и QA-блокировок. |
| `requires_prescription` | BOOLEAN | Рецептурный ветпрепарат. Влияет на каналы продажи (не выкладывается в самообслуживание) и на формирование заказа в магазины без ветврача. Для зоомагазина это реальная категория. |
| `is_dangerous_goods` | BOOLEAN | Аэрозоли, бытовая химия, антипаразитарные жидкости — товары с ограничениями на транспортировку (ADR). Фильтр для маршрутизации поставок. |
| `uktzed` | TEXT | код УКТ ВЭД (для импорта/растаможки). |
| `category_path` | TEXT | Денормализованный путь от корня иерархии (см. 5.1.2) до листовой категории, разделители — ` / `. Хранится **как строка** для быстрого LIKE-фильтра и group-by без рекурсивных JOIN. Источник истины — таблица `category` (там parent_id), `category_path` пересчитывается при каждой выгрузке товара. Примеры: `Корма / Сухие / Для собак / Премиум`, `Аксессуары / Поводки и ошейники / Поводки`, `Ветаптека / Антипаразитарные / Капли на холку`. Длина до 512 символов; уровней обычно 3–5. Для фильтра «всё в категории Корма» используется `category_path LIKE 'Корма / %'`. |
| `status` | TEXT ENUM | **Глобальный** статус карточки в каталоге сети, бинарный: <br>• **`active`** — товар закупается хотя бы в одном магазине сети. Карточка участвует в выгрузке, прогнозе и калькуляции. <br>• **`archived`** — товар нигде не закупается и не возвращается в ассортимент (полное выведение из сети). Карточка остаётся в БД для исторических расчётов и retention, но `/v1/store_assortment` для неё пуст. <br>Жизненный цикл «вводится / распродаётся / заблокирован» **не здесь** — это per-store состояние и живёт в `store_assortment.lifecycle_state` (см. §5.1.4), потому что один и тот же товар в магазине №5 может распродаваться, а в магазине №12 оставаться активным. Переход `active → archived` допустим только когда все строки `store_assortment` для этого товара в `discontinued` (агрегатный инвариант, проверяется application-level + ночным контролем). |
| `created_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Когда строка впервые появилась в нашей БД (момент первой успешной загрузки этого `product_id`). **Immutable on UPSERT** — `ON CONFLICT DO UPDATE` его не трогает. Не путать с моментом создания карточки в ERP — это разные времена. |
| `updated_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Когда последний раз реально изменилось хоть одно tracked-поле. Обновляется UPSERT-ом только при фактическом diff (через `WHERE` clause или триггер `BEFORE UPDATE`), а не на каждой выгрузке. Нужен для индексации «что изменилось за последние N дней» без скана changelog. |
| `load_id` | UUID FK | Snapshot, в котором карточка пришла последний раз. **Обновляется на каждом UPSERT**, даже если бизнес-поля не менялись — это маркер «строка была видна в свежем экспорте, не залежавшаяся». Используется для алерта «карточка не приходила N дней» (кандидат на архивацию). |

##### 5.1.1.a Баркоды товара (`product_barcodes`)
Связь N×1: одна товар → ≥1 баркодов. Используется для lookup по чекам с касс и приёмкам.

Страна-производитель живёт **здесь**, а не в карточке товара: одна и та же позиция (тот же `product_id`) может выпускаться в нескольких странах, и каждая партия имеет **свой EAN** (первые 2–3 цифры GS1-префикса как раз кодируют страну регистратора). Поэтому пара «баркод → страна» однозначна, а «товар → страна» — нет.

| Поле | Тип | Notes |
|---|---|---|
| `barcode` | TEXT PK | EAN-13 / EAN-8 / UPC / внутренний |
| `product_id` | TEXT FK | → `products.product_id` |
| `barcode_type` | TEXT ENUM | `ean13` / `ean8` / `upc` / `itf14` / `internal` |
| `is_primary` | BOOLEAN | основной баркод (отображается на ценнике, обычно соответствует consumer-единице с `pack_qty = 1`) |
| `country_origin` | TEXT | ISO-3166 alpha-2, страна-производитель этой партии/SKU-варианта |
| `pack_qty` | INTEGER NOT NULL DEFAULT 1 | **Множитель к consumer-единице.** `1` — баркод самой единицы продажи (банка, пачка, поводок). `>1` — баркод групповой упаковки: `24` — блок из 24 банок, `144` — коробка/паллет от поставщика. При продаже на кассе scan баркода с `pack_qty=N` означает продажу N consumer-единиц одной операцией. Источник истины о фасовке — здесь, не в `products`. |
| `created_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Когда баркод впервые попал в нашу БД. Immutable on UPSERT. Полезно для разбора кейсов «откуда взялся этот EAN» и для отчётов по новым баркодам за период. |
| `updated_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Обновляется только при фактическом diff (смена `pack_qty`, `is_primary`, `country_origin`). |
| `load_id` | UUID FK | Snapshot последнего seen, обновляется всегда. Если баркод уезжает на другой `product_id` — это **critical** в severity-движке (вероятный merge карточек / ошибка маппинга, требует ручного разбора). |

Уникальность: `barcode` глобально уникален в рамках snapshot. Если ERP вернула дубль (один EAN на двух товарх) — **critical** в severity-движке.

#### 5.1.2. Иерархия категорий (`category`)
| Поле | Тип | Notes |
|---|---|---|
| `category_id` | TEXT PK | |
| `parent_id` | TEXT FK NULL | |
| `level` | SMALLINT (1..N) | |
| `name` | TEXT | |
| `created_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Immutable on UPSERT (как в `products`). |
| `updated_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Обновляется только при реальном diff (`name`/`parent_id`/`level`). |
| `load_id` | UUID FK | Snapshot последнего seen, обновляется на каждом UPSERT. |

#### 5.1.3. Точки и склады (`location`)
| Поле | Тип | Notes |
|---|---|---|
| `location_id` | TEXT PK | |
| `type` | TEXT ENUM | store / warehouse / cross_dock / darkstore — **из критики §1.2** |
| `priority` | SMALLINT | приоритет по обороту — **из критики §1.2** |
| `gln` | TEXT | для EDI |
| `address` | TEXT | |
| `geo` | POINT | GPS для X-Flow логистики |
| `schedule` | JSONB | график работы |
| `edrpou_or_inn` | TEXT | юр.код |
| `created_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Immutable on UPSERT (как в `products`). |
| `updated_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Обновляется только при реальном diff (`type`/`priority`/`address`/`schedule`/...). |
| `load_id` | UUID FK | Snapshot последнего seen, обновляется на каждом UPSERT. |

#### 5.1.4. Ассортимент магазина (`store_assortment`) — пары товар × location
**Это ассортимент именно конкретного магазина** (или склада/даркстора — любой `location` нашей сети): какие товары числятся в данной торговой точке, плюс жизненный цикл этой связки. Один и тот же товар (`product_id`) обычно фигурирует во многих магазинах, но **в каждом — со своим состоянием** (где-то активен, где-то распродаётся, где-то ещё не введён, а в каких-то магазинах его нет вовсе). Это **не глобальный каталог сети** (для этого есть `products`) и **не остатки в штуках** (для этого есть `location_stock_snapshot` 5.2.2) — здесь только matrix-факт «товар × магазин» + lifecycle.

Отсутствие строки `(location_id, product_id)` означает **«никогда не было в этом магазине / never-stocked»** — это не статус, а отсутствие записи. Калькулятор такие пары не видит и не пытается заказать туда этот товар.

| Поле | Тип | Notes |
|---|---|---|
| `location_id` | FK → `location.location_id` | Магазин/склад/даркстор, в чьём ассортименте лежит товар. Пример: `STORE-005`. |
| `product_id` | FK → `products.product_id` | Карточка товара. Пример: `RC-ADULT-3KG` (Royal Canin для собак, 3 кг). |
| `min_shelf_presentation` | INTEGER | **MSP — минимальное товарное представление на полке.** Сколько штук должно быть видно покупателю в любой момент. Если фактический остаток падает ниже MSP — мерчандайзер докладывает, калькулятор повышает приоритет дозаказа. Пример: `MSP=4` для популярного корма (4 банки на полке всегда), `MSP=1` для дорогого редкого товара. |
| `facings` | INTEGER | **Фейсы — количество лицевых сторон товара на полке.** Влияет на скорость распродажи (визуальный объём) и на физическую вместимость. Пример: `facings=3` — товар занимает 3 «фасада» на стеллаже (3 в ряд × N в глубину). Для премиум-кормов часто `facings=1`, для дешёвых — `facings=4-6`. |
| `extra_display` | BOOLEAN | **Дополнительная выкладка** вне основной полки: торцевые стенды, прикассовая зона, паллет в проходе, сезонная выкладка. `true` — товар есть и в основной выкладке, и где-то ещё. Влияет на прогноз: extra-display даёт всплеск продаж +20–40%. Пример: на Новый год корма для собак ставят на торцевой стенд → `extra_display=true`. |
| `lifecycle_state` | TEXT ENUM NOT NULL DEFAULT `active` | **Per-store жизненный цикл пары.** Одно из: `pending` / `active` / `phasing_out` / `discontinued` (см. ниже). Пример: товар активен в магазине №5 (`active`), распродаётся в магазине №12 (`phasing_out`), не введён в магазин №20 (`pending`), выведен из №3 (`discontinued`). |
| `state_reason` | TEXT NULL | **Свободная причина последнего перехода lifecycle.** Заполняет категорийный менеджер или классификатор. Пример: `«пилот, тест на 4 магазинах»`, `«delisting под акцию Petshop»`, `«QA hold лот №2401, отзыв партии»`, `«сезон закрыт»`, `«ребрендинг — заменён на RC-ADULT-3KG-V2»`. |
| `introduced_at` | TIMESTAMPTZ NULL | **Дата ввода товара в этот магазин** (первая фактическая поставка или решение категорийщика). NULL пока `lifecycle_state='pending'` и физически ещё не приходило. Пример: `2026-03-15 00:00:00+00`. Используется для расчёта «новизны» (товар «new» = `now() - introduced_at < 28 дней`). |
| `discontinued_at` | TIMESTAMPTZ NULL | **Дата вывода из этого магазина.** NULL пока товар активен или распродаётся. Заполняется при переходе в `discontinued`. Пример: `2026-04-30 00:00:00+00`. После этой даты в `/v1/store_assortment?state=active` пара не возвращается. |
| `state_changed_at` | TIMESTAMPTZ NOT NULL | **Время последней смены `lifecycle_state`** (любой переход). Денормализация для быстрых запросов «что изменилось за последние N дней» без JOIN в журнал событий. Пример: `2026-05-01 14:23:00+00`. Если `lifecycle_state='active'` и `state_changed_at` > now() − 28d → товар считается «new». |
| `target_qty` | NUMERIC(10,3) NULL | **Целевой остаток на витрине + полке** (бизнес-цель, не факт). Калькулятор стремится заказать столько, чтобы `qty_on_hand + qty_in_transit ≈ target_qty + safety_stock`. `target_qty=0` сигналит run-out (используется классификатором для перевода в `phasing_out`). Пример: `target_qty=8` — держать 8 банок корма; `target_qty=0` — не закупать новые, распродать остаток. |
| `created_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Когда строка впервые появилась в нашей БД. Immutable on UPSERT. Пример: `2026-03-10 03:15:42+00` — это был день первого экспорта пары `(STORE-005, RC-ADULT-3KG)` из ERP. |
| `updated_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Когда последний раз реально изменилось бизнес-поле (MSP, facings, lifecycle_state, target_qty…). Не обновляется на «пустых» суточных загрузках без diff. |
| `load_id` | UUID FK → `loads.id` | Snapshot последнего seen. Обновляется на каждом UPSERT даже без diff (маркер «строка ещё видна в источнике»). Если `load_id` отстаёт от текущего на N дней — кандидат на архивацию. |
| **PK** | `(location_id, product_id)` | Одна строка на пару товар-магазин. Уникальность: один товар не может быть в одном магазине дважды. |

**Допустимые значения `lifecycle_state`:**
- **`pending`** — карточка зарегистрирована для магазина, но физически ещё не введена (нет первой поставки). Калькулятор: заказ по плану листинга, не по истории; safety stock завышен.
- **`active`** — нормальный режим, прогноз по истории продаж этой пары.
- **`phasing_out`** — товар распродаётся в этом магазине, новые заказы не формируются (`target_qty := 0`), маркдаун разрешён, lost-sales не считаем.
- **`discontinued`** — выведен из этого магазина, остаток выбыл (списан/вернули на РЦ/дораспродан). Строка остаётся для истории, но в выгрузке `/v1/store_assortment?state=active` не возвращается.

**Допустимые переходы** (CHECK + триггер):
```
pending      → active | discontinued
active       → phasing_out | discontinued
phasing_out  → discontinued | active   (повторное решение оставить)
discontinued → active                   (повторный ввод после паузы)
```
Запрещено: `active → pending`, `discontinued → phasing_out`, любой → `pending` после первого ввода.

**Что НЕ хранится колонкой, а вычисляется:**
- `is_new` = `lifecycle_state IN ('pending','active') AND state_changed_at > now() - interval '28 days'`. Окно «28 дней» — конфиг калькулятора, версионируется отдельно от схемы.
- `never_stocked` = отсутствие строки `(location_id, product_id)`.
- `out_of_season` — выводится из календаря категорий + текущей даты, не статус.

##### 5.1.4.a Журнал переходов жизненного цикла (`store_assortment_lifecycle_events`)
Append-only лог каждой смены `lifecycle_state`. **Это не SCD-2** (ADR-003 не нарушается): нет восстановления состояния на произвольную дату, только история событий для аудита и отчётов. Если позже понадобится полноценная bi-temporal история — backfill из этого журнала.

| Поле | Тип | Notes |
|---|---|---|
| `event_id` | UUID PK | Уникальный идентификатор события. Пример: `9f2d4c1a-8e3b-4c5f-9a1d-7b6e8f2a3c4d`. Генерится на нашей стороне (`gen_random_uuid()`), не из ERP. |
| `location_id` | TEXT FK → `location.location_id` | Магазин/склад, в котором произошёл переход. Пример: `STORE-005`. |
| `product_id` | TEXT FK → `products.product_id` | Карточка товара. Пример: `RC-ADULT-3KG`. Вместе с `location_id` ссылается на конкретную строку `store_assortment`. |
| `from_state` | TEXT NULL | Предыдущее значение `lifecycle_state`. **NULL только для первой записи** этой пары (когда товар впервые появился в магазине). Пример: `active`, `phasing_out`, `pending`. |
| `to_state` | TEXT NOT NULL | Новое значение `lifecycle_state`. Пример: `phasing_out` (переход active → phasing_out при решении вывести из магазина). |
| `changed_at` | TIMESTAMPTZ NOT NULL | Когда произошёл переход. Если изменение пришло из ERP — это время, указанное источником; если переход решили мы (auto_classifier) — `now()` на момент классификации. Пример: `2026-04-30 14:23:11+00`. |
| `changed_by` | TEXT | Кто инициировал переход. Допустимые форматы: <br>• `erp_sync` — пришло как факт из выгрузки ERP <br>• `category_manager:<user_id>` — явное решение категорийщика через UI / ручную правку в ERP. Пример: `category_manager:elena.k` <br>• `auto_classifier:<rule_id>` — наш классификатор по правилу из `lifecycle_rules.yaml`. Пример: `auto_classifier:rule_run_out_target_qty_zero` |
| `reason` | TEXT NULL | Бизнес-причина в свободной форме. Дублирует `store_assortment.state_reason` на момент перехода (для истории, если в текущей строке reason уже перезаписан). Примеры: `«delisting под Petshop»`, `«QA hold лот №2401»`, `«сезонная пауза до сентября»`, `«target_qty=0 в ERP 3 дня подряд»`. |
| `load_id` | UUID FK → `loads.id` | В каком прогоне выгрузки этот переход был зафиксирован. Пример: `8c1a4d2f-…`. Позволяет ответить «какие переходы попали в загрузку 2026-05-04 03:15». |

**Примеры строк:**

```
Сценарий: товар Royal Canin Adult 3kg в магазине STORE-005

(1) Ввод в магазин (первая запись):
  event_id = 9f2d4c1a-…
  location_id = STORE-005, product_id = RC-ADULT-3KG
  from_state = NULL,  to_state = pending
  changed_at = 2026-03-10 03:15:42+00
  changed_by = erp_sync
  reason = NULL

(2) Первая поставка пришла, перевели в active:
  from_state = pending,  to_state = active
  changed_at = 2026-03-15 09:00:00+00
  changed_by = auto_classifier:rule_first_receiving
  reason = «первая приёмка по DESADV-12345»

(3) Категорийщик решил вывести (под акцию конкурента):
  from_state = active,  to_state = phasing_out
  changed_at = 2026-04-30 14:23:11+00
  changed_by = category_manager:elena.k
  reason = «delisting под Petshop, перевод на собственную марку»

(4) Остаток дораспродан, выведен:
  from_state = phasing_out,  to_state = discontinued
  changed_at = 2026-06-01 22:00:00+00
  changed_by = auto_classifier:rule_qty_on_hand_zero
  reason = «qty_on_hand=0 7 дней подряд»

(5) Через год категорийщик вернул товар:
  from_state = discontinued,  to_state = pending
  changed_at = 2027-03-01 11:00:00+00
  changed_by = category_manager:elena.k
  reason = «возврат после переговоров с поставщиком»
```

Из такого журнала можно ответить на вопросы вроде:
- «Сколько SKU перешли в `phasing_out` за апрель?» → `WHERE to_state='phasing_out' AND changed_at BETWEEN ...`
- «Кто решил вывести этот товар?» → `WHERE location_id=... AND product_id=... AND to_state='discontinued'`
- «Какие переходы сделал авто-классификатор за вчера?» → `WHERE changed_by LIKE 'auto_classifier:%' AND changed_at >= ...`

#### 5.1.5. Поставщики (`supplier`)
Контрагенты, у которых E-Zoo закупает товар. Не путать с производителями (`products.manufacturer`) и брендами (`products.brand`): один поставщик может возить много брендов, и один бренд может приходить от нескольких поставщиков.

| Поле | Тип | Notes |
|---|---|---|
| `supplier_id` | TEXT PK | Внутренний ID поставщика в ERP E-Zoo. Стабильный, не меняется при ребрендинге юр.лица. Пример: `SUP-RC-UA` (для дистрибьютора Royal Canin в Украине), `SUP-LOCAL-042`. |
| `name` | TEXT | Юридическое или торговое наименование. Пример: `«ТОВ "Роял Канин Україна"»`, `«ФОП Іваненко О.М.»`. Используется в EDI-документах (раздел SELLER), отчётах закупки, переписке. |
| `edrpou` | TEXT NULL | Код ЄДРПОУ (для юрлиц) или ІПН (для ФОП). Используется для бухгалтерии и налоговой отчётности. Пример: `33547678` (8 цифр для юрлица), `1234567890` (10 цифр для ФОП). NULL допустим для иностранных поставщиков без украинской регистрации. |
| `gln` | TEXT NULL | Global Location Number (GS1, 13 цифр) — обязателен для EDI ORDERS / DESADV в качестве идентификатора BUYER/SELLER/SHIP-FROM. Один поставщик может иметь несколько GLN на разные склады отгрузки, но в карточке храним основной. Пример: `4820000000123`. NULL — если поставщик не подключён к EDI и обмен идёт через email/портал. |
| `payment_terms` | TEXT NULL | Условия оплаты в свободной форме (или по справочнику ERP). Влияет на cash-flow расчёты и приоритизацию поставщиков. Примеры: `«net30»` (оплата через 30 дней после поставки), `«predoplata 100%»`, `«net14 + 2% скидка при net7»`, `«реализация»` (оплата по факту продажи). |
| `created_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Когда строка впервые появилась в нашей БД. Immutable on UPSERT. |
| `updated_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Когда последний раз менялся `name` / `payment_terms` / GLN. |
| `load_id` | UUID FK → `loads.id` | Snapshot последнего seen. |

#### 5.1.7. Спецификации поставок (`supply_spec`) — **из критики §1.3**
Условия поставки конкретного товара от конкретного поставщика в конкретную локацию (магазин или РЦ). Один товар может закупаться:
- у нескольких поставщиков (тогда несколько строк с разным `priority`);
- одним поставщиком в разные локации с разными условиями (например, в Киев — паллетой, в регион — коробкой; разные цены, разные lead time);
- эта таблица — основа для калькуляции заказа: какому поставщику отдавать `ORDERS`, в каких количествах, по какой цене.

| Поле | Тип | Notes |
|---|---|---|
| `supplier_id` | TEXT FK → `supplier.supplier_id` | Поставщик. Пример: `SUP-RC-UA`. |
| `product_id` | TEXT FK → `products.product_id` | Товар. Пример: `RC-ADULT-3KG`. |
| `location_id` | TEXT FK → `location.location_id` | Куда поставщик отгружает: РЦ или конкретный магазин. Пример: `DC-KYIV-01` (РЦ Киев) либо `STORE-005` (для прямой поставки в магазин, минуя РЦ). |
| `min_order_qty` | INTEGER NOT NULL | **MOQ — минимальная партия заказа** в consumer-единицах (штуках). Калькулятор: если потребность < MOQ, либо ждём накопления, либо округляем вверх. Пример: `min_order_qty=24` (заказывать минимум блок из 24 банок), `min_order_qty=144` (минимум коробка), `min_order_qty=1` (поштучно, премиум). |
| `purchase_price` | NUMERIC(12,4) NULL | **Закупочная цена за consumer-единицу**, без НДС, в национальной валюте (UAH). Используется для расчёта стоимости заказа, бюджета закупки, маржи. NULL если поставщик ещё не зафиксировал прайс. Пример: `185.4500` UAH/шт. Точность 4 знака — для случаев импортных цен × курса. |
| `priority` | SMALLINT NOT NULL | **Приоритет поставщика по этому товару** для этой локации. `1` = первичный (используем по умолчанию), `2`, `3` = резервные (если первичный не может отгрузить, OOS у него, превышен лимит). Пример: для `RC-ADULT-3KG` в `DC-KYIV-01` приоритет 1 — `SUP-RC-UA` (официальный дистрибьютор), приоритет 2 — `SUP-LOCAL-042` (региональный дилер с наценкой). |
| `lead_time_days` | INTEGER NOT NULL | **Время от размещения заказа до приёмки**, в днях. Включает обработку поставщиком + доставку. Используется в формуле reorder point: `safety_stock + avg_demand × lead_time`. Пример: `lead_time_days=2` (срочная локальная поставка), `lead_time_days=14` (импорт через границу). |
| `pallet_units` | INTEGER NULL | **Сколько consumer-единиц на одной паллете отгрузки**. Может отличаться от `products.pallet_qty` (товар в карточке, поставщик может отгружать другой укладкой). Используется для расчёта лимита «паллет в поставке». Пример: `pallet_units=144` (12 коробок × 12 банок), `pallet_units=480` (мелкая фасовка). NULL — поставка не паллетная (поштучно/коробкой). |
| `created_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Immutable on UPSERT. |
| `updated_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Обновляется при изменении цены, MOQ, lead time, приоритета — то есть ключевые сигналы для пересчёта заказа. |
| `load_id` | UUID FK → `loads.id` | Snapshot последнего seen. |
| **PK** | (`supplier_id`, `product_id`, `location_id`) | Композитный — одна спецификация на тройку «поставщик-товар-локация». |

#### 5.1.8. Промо (`promo`)
Действующие и плановые промо-акции по товарам. Используется прогнозом для усиления спроса в период акции (lift +30..+200%) и калькуляцией для понимания «по какой цене сейчас продаётся товар». В MVP — **без per-store разреза**: промо считается сетевым (действует во всех магазинах из `store_assortment`). Per-store промо — отложено (если бизнес подтвердит — добавим `location_id` или отдельную таблицу `promo_scope`).

| Поле | Тип | Notes |
|---|---|---|
| `promo_id` | TEXT PK | Внутренний ID промо в ERP. Одна строка = одна пара «промо × товар»: если акция «−20% на все корма Royal Canin» покрывает 50 SKU, в таблице 50 строк с разными `promo_id` (либо одно сложное `promo_id`, отражающее заголовок). Пример: `PROMO-2026-05-RC-ADULT`. |
| `product_id` | TEXT FK → `products.product_id` | Товар, на который распространяется акция. Пример: `RC-ADULT-3KG`. |
| `type` | TEXT ENUM | Тип механики акции. Допустимые значения: <br>• `markdown` — снижение цены (самое частое). Пример: 285 → 229 UAH. <br>• `bogo` — buy-one-get-one (купи N, получи M бесплатно). <br>• `bundle` — комплект из нескольких SKU по специальной цене. <br>• `loyalty_bonus` — повышенные баллы лояльности (цена та же, но больше бонусов). <br>• `gift` — подарок при покупке (носочки/игрушка к корму). <br>В MVP калькуляция учитывает в основном `markdown` (есть `promo_price_with_vat`); остальные типы — для прогноза lift и для маркетинговой отчётности. |
| `date_from` | DATE NOT NULL | Дата начала акции (включительно). Пример: `2026-05-01`. |
| `date_to` | DATE NOT NULL | Дата окончания (включительно). Пример: `2026-05-14`. Если акция бессрочная — далёкая дата (например `2099-12-31`), а не NULL — упрощает запросы `WHERE current_date BETWEEN date_from AND date_to`. |
| `promo_price_with_vat` | NUMERIC(12,2) NULL | Промо-цена реализации **с НДС**, в UAH за consumer-единицу. Используется как фактическая цена в период акции. Для типов `bogo`/`gift`/`loyalty_bonus` обычно NULL (цена не меняется, меняется механика). Пример: `229.00` (вместо обычной 285.00). |
| `created_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Immutable on UPSERT. |
| `updated_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Обновляется при изменении цены, дат или типа. |
| `load_id` | UUID FK → `loads.id` | Snapshot последнего seen. |

**Пример строк:**

```
(1) Акция «−20% на Royal Canin Adult 3kg» — две недели в мае:
  promo_id = PROMO-2026-05-RC-ADULT
  product_id = RC-ADULT-3KG
  type = markdown
  date_from = 2026-05-01,  date_to = 2026-05-14
  promo_price_with_vat = 229.00  (вместо обычной 285.00)

(2) BOGO «3 банки Whiskas по цене 2» — выходные:
  promo_id = PROMO-2026-05-WHISKAS-BOGO
  product_id = WHISKAS-CAT-100G
  type = bogo
  date_from = 2026-05-04,  date_to = 2026-05-05
  promo_price_with_vat = NULL   (цена за банку не меняется)

(3) Бессрочный лоялти-бонус +5% баллов на премиум-корма:
  promo_id = LOYALTY-PREMIUM-PERMANENT
  product_id = RC-ADULT-3KG
  type = loyalty_bonus
  date_from = 2026-01-01,  date_to = 2099-12-31
  promo_price_with_vat = NULL
```

#### 5.1.9. Типы поставок (`supply_type`)
ENUM: `direct` / `cross_dock` / `dc` / `dropship`.

#### 5.1.10. Бизнес-правила заказов (`order_rule`)
Декларативные правила, которые модулю калькуляции говорят «как считать заказ» и «какие лимиты соблюдать». Хранятся как структурированный JSON, а не как код, чтобы категорийщик мог редактировать правила без релиза. Применяются по приоритету: правило с более узким `scope` (конкретный SKU) перекрывает более широкое (вся категория).

| Поле | Тип | Notes |
|---|---|---|
| `rule_id` | TEXT PK | Внутренний ID правила. Пример: `RULE-DRY-FOOD-DEFAULT`, `RULE-RC-PREMIUM-OVERRIDE`, `RULE-DC-KYIV-PALLET-LIMIT`. |
| `scope` | JSONB NOT NULL | На какие пары `(location, product)` действует. Поля внутри JSON: `location_ids`, `category_paths`, `product_ids`, `brands` — любое сочетание. NULL/отсутствие поля = «всё». Чем уже scope, тем выше приоритет. |
| `formula` | TEXT NOT NULL | DSL-формула расчёта целевого заказа. На MVP — узкий набор именованных формул (`reorder_point`, `min_max`, `econ_order_qty`) с параметрами в `formula_params` (часть JSON). Если усложнится — отдельный raw-DSL файл. |
| `formula_params` | JSONB NULL | Параметры формулы (зависят от типа): `safety_days`, `service_level`, `review_days`, `min_qty`, `max_qty`. |
| `limits` | JSONB NULL | Жёсткие ограничения на итоговый заказ. Поля: `max_amount_uah`, `max_pallets`, `max_weight_kg`, `max_volume_m3`, `max_order_lines`. Применяются как cap (если расчёт даёт больше — урезаем по приоритетам ABC). |
| `priority` | SMALLINT NOT NULL DEFAULT 100 | Порядок применения. Меньше число = раньше. Если два правила пересекаются по scope, побеждает с меньшим `priority`. |
| `created_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Immutable on UPSERT. |
| `updated_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Обновляется при изменении формулы/scope/limits. |
| `load_id` | UUID FK → `loads.id` | Snapshot последнего seen. |

**Примеры строк:**

```
(1) Дефолтное правило для всех сухих кормов:
  rule_id = RULE-DRY-FOOD-DEFAULT
  scope = {"category_paths": ["Корма / Сухие / %"]}
  formula = reorder_point
  formula_params = {"safety_days": 7, "service_level": 0.95, "review_days": 1}
  limits = NULL
  priority = 100

(2) Узкий override для премиум-RC (более жёсткие safety + ограничение по max):
  rule_id = RULE-RC-PREMIUM-OVERRIDE
  scope = {"brands": ["Royal Canin"], "category_paths": ["Корма / Сухие / Для собак / Премиум"]}
  formula = min_max
  formula_params = {"min_qty": 4, "max_qty": 30, "safety_days": 10}
  limits = {"max_amount_uah": 80000}
  priority = 50           ← перекрывает (1) для пересечения

(3) Лимит на одну поставку в РЦ Киев (паллеты + сумма):
  rule_id = RULE-DC-KYIV-PALLET-LIMIT
  scope = {"location_ids": ["DC-KYIV-01"]}
  formula = pass_through    ← без своей формулы, только лимит
  formula_params = NULL
  limits = {"max_pallets": 33, "max_weight_kg": 22000, "max_amount_uah": 1500000}
  priority = 10            ← применяется в самом конце как cap

(4) Жёсткое правило для рецептурных ветпрепаратов (минимум на полке = 1):
  rule_id = RULE-RX-MIN-1
  scope = {"category_paths": ["Ветаптека / Рецептурные / %"]}
  formula = min_max
  formula_params = {"min_qty": 1, "max_qty": 5, "safety_days": 14}
  limits = NULL
  priority = 30
```

**Как применяются:** для пары `(STORE-005, RC-ADULT-3KG)` сначала находим все правила с подходящим `scope`, сортируем по `priority`, применяем по очереди (более узкое перекрывает более широкое в части параметров формулы). Лимиты cap'ом накладываются в конце на итоговый заказ по магазину/локации.

#### 5.1.11. План поставок (`supply_plan`)
Расписание поставок: какой поставщик в какие дни недели возит товар в какую локацию, и до какого времени надо успеть отправить заказ. Используется калькулятором, чтобы знать «когда формировать ORDERS» и «когда придёт товар» — критично для расчёта окна потребности.

| Поле | Тип | Notes |
|---|---|---|
| `location_id` | TEXT FK → `location.location_id` | Куда поставщик возит. Пример: `DC-KYIV-01` (РЦ Киев) или `STORE-005` (прямая поставка в магазин). |
| `supplier_id` | TEXT FK → `supplier.supplier_id` | Кто возит. Пример: `SUP-RC-UA`. |
| `weekday` | SMALLINT (1..7) | День недели поставки (1=пн, 7=вс). Пример: `2` — поставщик возит по вторникам. Если возит несколько дней в неделю — несколько строк. |
| `cutoff_time` | TIME NOT NULL | **Дедлайн отправки заказа** в день D-`delivery_lead_days`. До этого времени надо успеть отправить ORDERS, иначе поставка съедет на следующий слот расписания. Пример: `cutoff_time=14:00` — заказ должен быть в EDI до 14:00 в день, на N дней опережающий день поставки. |
| `delivery_lead_days` | INTEGER NOT NULL | Через сколько дней после cutoff приедет товар. Не путать с `supply_spec.lead_time_days` (тот — общий lead time поставщика); здесь — конкретно для этого слота расписания. Пример: `delivery_lead_days=2` (заказ во вторник до 14:00 → поставка в четверг). |
| `schedule_periodicity` | TEXT NOT NULL | **Периодичность** работы этого слота. Допустимые значения: <br>• `weekly` — каждую неделю (стандарт). <br>• `biweekly` — раз в 2 недели (для редких/малооборотных товаров). <br>• `monthly_first` — первый указанный weekday месяца. <br>• `monthly_last` — последний указанный weekday месяца. <br>• `seasonal` — только в указанный сезонный период (требует доп. полей `season_from`/`season_to`, отложено в MVP). <br>**Из критики diagram_review §2.3.** Пример: `weekly` — еженедельно по вторникам; `biweekly` — раз в 2 недели по пятницам. |
| `created_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Immutable on UPSERT. |
| `updated_at` | TIMESTAMPTZ NOT NULL DEFAULT now() | Обновляется при изменении cutoff/lead/периодичности. |
| `load_id` | UUID FK → `loads.id` | Snapshot последнего seen. |
| **PK** | (`location_id`, `supplier_id`, `weekday`) | Композитный — одна строка на «локация × поставщик × день недели». |

**Примеры строк:**

```
(1) Поставщик Royal Canin возит в РЦ Киев по вторникам и пятницам:
  location_id = DC-KYIV-01,  supplier_id = SUP-RC-UA
  weekday = 2 (вт),  cutoff_time = 14:00,  delivery_lead_days = 2,  schedule_periodicity = weekly
  → заказ в воскресенье до 14:00, поставка во вторник
  weekday = 5 (пт),  cutoff_time = 14:00,  delivery_lead_days = 2,  schedule_periodicity = weekly
  → заказ в среду до 14:00, поставка в пятницу

(2) Местный поставщик возит прямо в магазин по средам:
  location_id = STORE-005,  supplier_id = SUP-LOCAL-042
  weekday = 3 (ср),  cutoff_time = 18:00,  delivery_lead_days = 1,  schedule_periodicity = weekly
  → заказ во вторник до 18:00, поставка в среду утром

(3) Малооборотный экзотический корм (раз в 2 недели):
  location_id = DC-KYIV-01,  supplier_id = SUP-EXOTIC-IMPORT
  weekday = 4 (чт),  cutoff_time = 12:00,  delivery_lead_days = 7,  schedule_periodicity = biweekly
  → заказ в среду чётной недели до 12:00, поставка в четверг следующей недели

(4) Сезонные новогодние подарочные наборы (только в декабре):
  location_id = DC-KYIV-01,  supplier_id = SUP-XMAS
  weekday = 1 (пн),  cutoff_time = 10:00,  delivery_lead_days = 3,  schedule_periodicity = seasonal
  → действует только в декабре (доп. season_from/season_to — TODO)
```

**Как используется калькулятором:**
- На дату D калькулятор находит ближайший подходящий слот (по поставщику и локации).
- Считает «следующий cutoff» = ближайший день недели по `weekday` с учётом `schedule_periodicity`, в `cutoff_time`.
- Окно потребности = `cutoff + delivery_lead_days` до следующей поставки.
- Если потребность превышает MOQ (`supply_spec.min_order_qty`) и попадает в окно — формирует ORDERS к этому cutoff.

### 5.2. Факты (статистика, §5.2 ТЗ) — все bi-temporal

#### 5.2.1. Чеки (`receipt_line`) — append-only events
Строки кассовых чеков. Одна строка таблицы = один товар в одном чеке. Для bi-temporal коррекций (см. ниже) одна логическая строка чека может иметь несколько физических версий с разными `system_time_from`. Никогда не UPDATE, только INSERT — старые версии «закрываются» через `system_time_to`.

| Поле | Тип | Notes |
|---|---|---|
| `receipt_id` | TEXT NOT NULL | Идентификатор чека (от кассы / фискального регистратора). Уникален в рамках смены/локации. Пример: `RCPT-STORE-005-20260504-001234`. |
| `line_no` | SMALLINT NOT NULL | Порядковый номер строки в чеке (1, 2, 3…). Вместе с `receipt_id` идентифицирует логическую строку. |
| `location_id` | TEXT FK → `location.location_id` | В каком магазине пробит чек. Пример: `STORE-005`. |
| `product_id` | TEXT FK → `products.product_id` | Какой товар продан. Резолвится по `barcode_scanned` через `product_barcodes`. Пример: `RC-ADULT-3KG`. |
| `barcode_scanned` | TEXT NULL | Фактический EAN, считанный сканером кассы. Нужен для аудита и для разрешения мульти-баркодов (один товар, разные EAN на банке/блоке/коробке). Пример: `4607101234567`. NULL — если кассир ввёл товар вручную по коду. |
| `qty` | NUMERIC(10,3) NOT NULL | Количество в consumer-единицах. Дробное для весовых товаров. Положительное — продажа, **отрицательное — возврат** (отдельной строкой, не корректировкой). Примеры: `2.000` (две банки), `0.345` (345 г сухого корма на развес), `-1.000` (возврат одной банки). |
| `line_kind` | TEXT ENUM NOT NULL DEFAULT `sale` | Тип строки чека: <br>• `sale` — обычная продажа (с скидкой или без). <br>• `return` — возврат покупателя (qty < 0). <br>• `gift` — подарочная позиция (товар отдан бесплатно вне промо-механики, например, маркетинговый подарок). <br>• `promo_bonus` — бесплатная позиция в рамках конкретной акции (BOGO «вторая бесплатно», «купи 2 — третий в подарок» и т.п.). Отличается от `gift` тем, что **связан с `promo_id`** и предсказуем для прогноза lift-эффекта. |
| `unit_price_base` | NUMERIC(12,2) NOT NULL | **Базовая цена за consumer-единицу с НДС**, до любых скидок. Используется для расчёта суммы скидок, маржи, базового оборота. Пример: `285.00`. Для подарка/бонуса — базовая цена товара (НЕ ноль), чтобы видеть реальную «отдачу» по стоимости. |
| `unit_price_paid` | NUMERIC(12,2) NOT NULL | **Фактическая цена за consumer-единицу**, по которой клиент заплатил. Для подарка/бонуса — `0.00`. Для скидочной продажи — цена со скидкой. Пример: `229.00` (markdown), `0.00` (gift/promo_bonus), `285.00` (без скидки). |
| `discount_amount` | NUMERIC(12,2) NOT NULL DEFAULT 0 | Суммарная скидка на эту строку, всегда `>= 0`. Инвариант: `discount_amount = qty × (unit_price_base − unit_price_paid)`. Хранится материализованно для быстрых отчётов и checksum-проверки целостности импорта. |
| `markdown_pct` | NUMERIC(5,2) NULL | Информационное поле: процент уценки/маркдауна, если приходит из ERP отдельно (не для расчёта — для аналитики). Может вычисляться из base/paid, но дублируется на случай если в ERP проставлен иначе (например, маркдаун = 20%, но фактическая цена дополнительно скруглена до 229.00). |
| `promo_id` | TEXT FK → `promo.promo_id` NULL | Связь со конкретной акцией, по которой произошла скидка/подарок. NULL — продажа без акции (полная цена) или ручная скидка кассира. Для `line_kind` ∈ (`promo_bonus`) — **NOT NULL** (бонус всегда привязан к акции). |
| `event_time` | TIMESTAMPTZ NOT NULL | **Момент пробития чека на кассе** (часы кассы). Используется для прогноза («продажи по часам / дням»), партиционирования и алерта на future-dated. Пример: `2026-05-04 17:23:11+02`. |
| `loyalty_hash` | TEXT NULL | SHA-256 хеш `loyalty_id || tenant_salt`. Позволяет считать «повторные покупатели» без хранения PII. NULL — покупка без карты лояльности. |
| `valid_from` | TIMESTAMPTZ NOT NULL | Бизнес-время начала действия факта. Для чеков обычно равен `event_time`. |
| `valid_to` | TIMESTAMPTZ NOT NULL DEFAULT `'infinity'` | Бизнес-время конца. `infinity` = факт всё ещё актуален; конкретная дата — факт «закрыт» (например, чек отменён). |
| `system_time_from` | TIMESTAMPTZ NOT NULL | Когда строка попала в нашу БД. |
| `system_time_to` | TIMESTAMPTZ NOT NULL DEFAULT `'infinity'` | Когда строка была заменена коррекцией. `infinity` = текущая актуальная версия. |
| `load_id` | UUID FK → `loads.id` | В каком прогоне выгрузки эта версия пришла. |
| **PK** | (`receipt_id`, `line_no`, `system_time_from`) | Композитный — позволяет хранить несколько физических версий одной логической строки. |
| **Partition** | BY RANGE (`event_time`) | Дневные партиции — критично для retention sweeper и быстрых запросов «продажи за вчера». |

**Пример 1 — обычный чек на два товара (промо-скидка + полная цена):**

```
event_time = 2026-05-04 17:23:11+02 в магазине STORE-005,
покупатель купил банку Royal Canin по акции и пакет наполнителя без скидки.

Строка 1 (промо-скидка):
  receipt_id = RCPT-STORE-005-20260504-001234,  line_no = 1
  location_id = STORE-005,  product_id = RC-ADULT-3KG
  barcode_scanned = 4607101234567
  qty = 1.000,  line_kind = sale
  unit_price_base = 285.00,  unit_price_paid = 229.00,  discount_amount = 56.00
  markdown_pct = 20.00,  promo_id = PROMO-2026-05-RC-ADULT
  event_time = 2026-05-04 17:23:11+02
  loyalty_hash = a3f8c1...
  valid_from = 2026-05-04 17:23:11+02,  valid_to = infinity
  system_time_from = 2026-05-05 03:15:42+00,  system_time_to = infinity
  load_id = 8c1a4d2f-…

Строка 2 (полная цена, без акции):
  receipt_id = RCPT-STORE-005-20260504-001234,  line_no = 2
  product_id = LITTER-CAT-5KG
  qty = 1.000,  line_kind = sale
  unit_price_base = 145.00,  unit_price_paid = 145.00,  discount_amount = 0.00
  markdown_pct = NULL,  promo_id = NULL
  ...
```

**Пример 2 — продажа на развес + возврат отдельной строкой:**

```
event_time = 2026-05-05 11:08:33+02, STORE-012:

Строка 1 (развесной корм 345 г):
  receipt_id = RCPT-STORE-012-20260505-000456,  line_no = 1
  product_id = DRY-FOOD-BULK
  barcode_scanned = NULL  (взвешивал кассир, баркод от весов)
  qty = 0.345,  line_kind = sale
  unit_price_base = 259.42,  unit_price_paid = 259.42,  discount_amount = 0.00
  (итого по строке: 0.345 × 259.42 ≈ 89.50 UAH)
  promo_id = NULL

Строка 2 (возврат банки RC из вчерашней покупки):
  receipt_id = RCPT-STORE-012-20260505-000456,  line_no = 2
  product_id = RC-ADULT-3KG
  qty = -1.000,  line_kind = return
  unit_price_base = 285.00,  unit_price_paid = 229.00,  discount_amount = 56.00
  (возвращаем по той же цене со скидкой; деньги клиенту: 1 × 229 = 229 UAH)
  promo_id = PROMO-2026-05-RC-ADULT
```

**Пример 3 — BOGO «2 банки по цене 1»:**

```
event_time = 2026-05-04 18:45:22+02, STORE-005:

Строка 1 (полная цена):
  receipt_id = RCPT-...-001235,  line_no = 1
  product_id = RC-ADULT-3KG,  qty = 1.000,  line_kind = sale
  unit_price_base = 285.00,  unit_price_paid = 285.00,  discount_amount = 0.00
  promo_id = PROMO-BOGO-RC-MAY

Строка 2 (вторая банка бесплатно по той же акции):
  receipt_id = RCPT-...-001235,  line_no = 2
  product_id = RC-ADULT-3KG,  qty = 1.000,  line_kind = promo_bonus
  unit_price_base = 285.00,  unit_price_paid = 0.00,  discount_amount = 285.00
  promo_id = PROMO-BOGO-RC-MAY  (NOT NULL — обязательно для promo_bonus)
```

Аналитика:
- «Сколько RC мы продали по факту?» → `SUM(qty) WHERE line_kind IN ('sale','promo_bonus')` = 2 банки (физический отток со склада).
- «Сколько денег получили?» → `SUM(qty × unit_price_paid)` = 285 UAH.
- «Сколько отдали в виде акционной нагрузки?» → `SUM(discount_amount) WHERE promo_id IS NOT NULL` = 285 UAH.

**Пример 4 — «купи 2, третий в подарок»:**

```
event_time = 2026-05-06 12:10:00+02, STORE-008:

Строка 1, 2 (две банки по полной цене):
  product_id = RC-ADULT-3KG,  qty = 1.000,  line_kind = sale
  unit_price_base = 285.00,  unit_price_paid = 285.00,  discount_amount = 0.00
  promo_id = PROMO-3FOR2-RC-MAY

Строка 3 (третья — подарок в рамках той же акции):
  product_id = RC-ADULT-3KG,  qty = 1.000,  line_kind = promo_bonus
  unit_price_base = 285.00,  unit_price_paid = 0.00,  discount_amount = 285.00
  promo_id = PROMO-3FOR2-RC-MAY
```

**Пример 5 — маркетинговый подарок вне акции (не промо, разовое решение менеджера):**

```
Магазин дал бесплатный поводок по жалобе клиента:

Строка 1 (поводок):
  product_id = LEASH-NYLON-1.5M,  qty = 1.000,  line_kind = gift
  unit_price_base = 89.00,  unit_price_paid = 0.00,  discount_amount = 89.00
  promo_id = NULL  (gift не привязан к акции)
```

Отличие `gift` от `promo_bonus`: `gift` — нерегулярное событие, не учитывается в lift-моделях прогноза. `promo_bonus` — предсказуемая часть механики, моделируется отдельно.

**Пример 6 — bi-temporal коррекция (чек отредактировали в ERP задним числом).** Кассирша через час замечает, что ввела `qty=2` вместо `qty=1`, или менеджер корректирует чек.

```
Изначально 04 мая в 17:23 пробит чек с RC-ADULT-3KG, qty=2.
Утром 05 мая выгрузка получила запись (load L1):

  receipt_id = RCPT-STORE-005-20260504-001234,  line_no = 1
  product_id = RC-ADULT-3KG,  qty = 2.000,  line_kind = sale
  unit_price_base = 285.00,  unit_price_paid = 229.00,  discount_amount = 112.00
  promo_id = PROMO-2026-05-RC-ADULT
  event_time = 2026-05-04 17:23:11+02
  valid_from = 2026-05-04 17:23:11+02,  valid_to = infinity
  system_time_from = 2026-05-05 03:15:42+00,  system_time_to = infinity   ← АКТУАЛЬНА
  load_id = L1

Днём 05 мая ERP прислал коррекцию: правильное qty=1.
Вечером 05 мая выгрузка увидела diff и:
  (а) закрыла старую версию: SET system_time_to = '2026-05-05 21:00:00+00' WHERE pk
  (б) вставила новую строку:

Строка А (старая, исторический след — что мы ВИДЕЛИ днём):
  ... qty = 2.000,  discount_amount = 112.00
  system_time_from = 2026-05-05 03:15:42+00
  system_time_to   = 2026-05-05 21:00:00+00       ← закрыта

Строка B (новая, текущая правда):
  ... qty = 1.000,  discount_amount = 56.00      ← поправлено (1 × 56 = 56)
  event_time = 2026-05-04 17:23:11+02            ← бизнес-время не меняется
  system_time_from = 2026-05-05 21:00:00+00
  system_time_to   = infinity                    ← АКТУАЛЬНА
  load_id = L2
```

Что это даёт:
- **«Текущие продажи за 4 мая»** → `WHERE event_time::date = '2026-05-04' AND system_time_to = 'infinity'` → строка B (qty=1).
- **«Что мы знали о продажах 4 мая утром 5 мая»** → `WHERE system_time_from <= '2026-05-05 12:00' AND system_time_to > '2026-05-05 12:00'` → строка A (qty=2). Прогноз, построенный тогда, остаётся объяснимым.

Отмена чека целиком — другой кейс: вставляются коррекции со всеми `qty=0` или специальный флаг отмены. Старые версии закрываются аналогично.

#### 5.2.2. Остатки (`location_stock_snapshot`) — daily snapshot per (товар × location × date)
| Поле | Тип |
|---|---|
| `snapshot_date` | DATE |
| `location_id` × `product_id` | composite key |
| `qty_on_hand` | NUMERIC(12,3) |
| `qty_reserved` | NUMERIC(12,3) |
| `qty_in_transit` | NUMERIC(12,3) |
| `valid_from`, `valid_to` | TIMESTAMPTZ — на какой период относится снимок (обычно сутки `snapshot_date` 00:00 → +24h) |
| `system_time_from`, `system_time_to` | TIMESTAMPTZ — когда снимок появился/был перезаписан коррекцией |
| `load_id` | UUID FK |

#### 5.2.3. Перемещения, списания, инвентаризации (`stock_movement`)
| Поле | Тип |
|---|---|
| `movement_id` | TEXT PK |
| `type` | ENUM (transfer / write_off / inventory / receiving) |
| `location_from`, `location_to` | NULLable FK |
| `product_id` | FK |
| `qty` | NUMERIC(12,3) |
| `event_time` | TIMESTAMPTZ — фактический момент движения |
| `valid_from`, `valid_to` | TIMESTAMPTZ — бизнес-период действия записи |
| `system_time_from`, `system_time_to` | TIMESTAMPTZ — когда строка появилась/была заменена в БД |
| `load_id` | UUID FK |

#### 5.2.4. Остатки у поставщика (`supplier_stock_snapshot`)
Снапшот доступности товара **на стороне поставщика** (то, что поставщик готов отгрузить, плюс плановые приходы). Симметрично `location_stock_snapshot`, но измерение — поставщик, а не наша локация. Используется калькулятором, чтобы:
- не размещать ORDERS на больший объём, чем поставщик физически может дать (capacity-aware ordering);
- предсказать дефицит у поставщика заранее (если `qty_available` падает ниже нашей средней потребности);
- поддержать VMI-режим (Vendor-Managed Inventory), где поставщик сам видит потребление и планирует поставки.

**Источник данных** зависит от поставщика — может приходить через EDI **INVRPT** (Inventory Report), через REST API дистрибьютора, через Excel-выгрузку, или вообще не приходить (тогда таблица пустая для этого поставщика — это OK, калькулятор работает в режиме «assume infinite supply»). Какой способ использует E-Zoo и какие поставщики поддерживают — открытый вопрос (OQ-12).

| Поле | Тип | Notes |
|---|---|---|
| `snapshot_date` | DATE NOT NULL | Дата снимка. Снапшоты обычно ежедневные. Пример: `2026-05-05`. |
| `supplier_id` | TEXT FK → `supplier.supplier_id` | Поставщик. Пример: `SUP-RC-UA`. |
| `product_id` | TEXT FK → `products.product_id` | Товар. Пример: `RC-ADULT-3KG`. |
| `qty_available` | NUMERIC(12,3) NOT NULL | **Доступно к отгрузке прямо сейчас**, в consumer-единицах. Это то, что поставщик готов взять в наш ORDERS без задержки. Пример: `qty_available=4800` (40 коробок по 120 банок). |
| `qty_reserved_for_us` | NUMERIC(12,3) NULL | Уже зарезервировано под наши размещённые ORDERS, ещё не отгружено. NULL если поставщик не делит по клиентам. Пример: `qty_reserved_for_us=1200`. |
| `qty_in_production` | NUMERIC(12,3) NULL | В производстве у поставщика, поступит на склад поставщика к `expected_replenishment_date`. NULL если поставщик не открывает эту информацию. Пример: `qty_in_production=12000`. |
| `expected_replenishment_date` | DATE NULL | Когда ожидается следующее пополнение склада поставщика. Используется для расчёта «дотянем ли до новой партии». Пример: `2026-05-20`. |
| `currency` | TEXT NULL | Валюта закупочной цены (если поставщик присылает цену в снапшоте). Обычно UAH; для импорта может быть USD/EUR. NULL — если цена не передаётся в snapshot (берём из `supply_spec`). |
| `unit_price` | NUMERIC(12,4) NULL | Закупочная цена за consumer-единицу на момент snapshot (если поставщик прислал актуальную). NULL — используется `supply_spec.purchase_price`. Пример: `185.4500`. |
| `valid_from`, `valid_to` | TIMESTAMPTZ | Бизнес-время действия snapshot. Обычно `valid_from = snapshot_date 00:00`, `valid_to = snapshot_date + 1 day` или `infinity` до прихода нового снимка. |
| `system_time_from`, `system_time_to` | TIMESTAMPTZ | Когда строка появилась/была заменена коррекцией в нашей БД. |
| `load_id` | UUID FK → `loads.id` | В каком прогоне выгрузки snapshot пришёл. |
| **PK** | (`snapshot_date`, `supplier_id`, `product_id`, `system_time_from`) | Композитный — позволяет хранить bi-temporal коррекции. |
| **Partition** | BY RANGE (`snapshot_date`) | Дневные партиции, как у `location_stock_snapshot`. |

**Пример строки:**

```
snapshot_date = 2026-05-05
supplier_id = SUP-RC-UA,  product_id = RC-ADULT-3KG
qty_available = 4800        ← поставщик готов дать сегодня 4800 банок
qty_reserved_for_us = 1200  ← из них 1200 уже под наш размещённый ORDERS
qty_in_production = 12000   ← в производстве, поступит к 2026-05-20
expected_replenishment_date = 2026-05-20
currency = UAH,  unit_price = 185.4500
valid_from = 2026-05-05 00:00,  valid_to = infinity
system_time_from = 2026-05-05 04:30,  system_time_to = infinity
load_id = 8c1a4d2f-…
```

**Что это даёт калькулятору:**
- Если наш заказ хочет 6000 шт, а `qty_available - qty_reserved_for_us = 3600` — режем заказ до 3600 + ставим алерт «недостаточно у поставщика, нужны альтернативы».
- Если `qty_available` стабильно падает на N единиц/день и не пополняется до `expected_replenishment_date` — заранее переключаемся на резервного поставщика (см. `supply_spec.priority`).

### 5.3. Служебные таблицы

| Таблица | Поля |
|---|---|
| `loads` | `id UUID PK`, `started_at`, `finished_at`, `status` (running/committed/failed), `source`, `entities_summary JSONB` |
| `snapshot_pointer` | `current_load_id UUID`, `previous_load_id UUID`, `committed_at` (single row) |
| `reject_log` | `load_id FK`, `entity TEXT`, `pk_value JSONB`, `severity`, `reason TEXT`, `raw JSONB`, `detected_at` |
| `entity_checkpoint` | `load_id`, `entity`, `cursor JSONB`, `rows_done BIGINT` (для resume в рамках одного load) |
| `audit_access` | `requester TEXT`, `endpoint TEXT`, `query JSONB`, `bytes_out BIGINT`, `ts` (для IT E-Zoo) |

---

## 6. Non-functional requirements

| Атрибут | Целевое значение |
|---|---|
| **KPI** | Полнота 100% за сутки (rows_in_source = rows_in_target) |
| **SLA окна** | Завершение до 06:00 локального времени (cutoff Replenishment) |
| **Объём** | >500K SKU, >1000 локаций, >10M receipt_line/сутки |
| **Retention** | 30 дней hot в PG, 365 дней cold в S3 (Parquet) |
| **Auth (источник + потребитель)** | **TBD** — open question для ИБ E-Zoo (OAuth2 cc / mTLS / API-key + IP allowlist) |
| **Multi-tenancy** | Single-tenant навсегда (нет `tenant_id`) |
| **Топология** | Хост в cloud X-Flow; ERP — через VPN site-to-site |

---

## 7. Architecture & ADRs

### ADR-001 — Адаптер на нашей стороне (pull)
**Решение:** реализуем pull-адаптер на Go, который ходит в REST API ERP E-Zoo. E-Zoo не пишет outbound-логику.
**Trade-off:** контроль на нашей стороне vs нагрузка на ERP API. Принято: меньше зависимостей от E-Zoo dev-команды.

### ADR-002 — Materialize в PG18 (НЕ stream-through)
**Решение:** материализуем канонические таблицы в свой PG18.
**Trade-off:** хранение vs воспроизводимость. Принято: полнота, audit, восстанавливаемость > экономия на диске.

### ADR-003 — Bi-temporal только для фактов
**Решение:** факты (`receipt_line`, `location_stock_snapshot`, `stock_movement`) хранятся с `valid_time` + `system_time`. Мастер-данные — только current state (SCD-2 откладываем).
**Trade-off:** в первой итерации потеряем историю переименований товара, переноса баркодов между карточками и слияний категорий, но избежим x10 объёма master-данных.

### ADR-004 — REST-контракт с content-negotiation
**Решение:**
- Малые ответы (мастер-данные, `< 50 MB`) → `application/x-ndjson` inline + курсорная пагинация (`Link: rel=next`).
- Большие (`receipts`, `stock_movement`) → async-job: `POST /v1/exports {entity, snapshot_id}` → polling `GET /v1/exports/{id}` → 303 redirect на signed URL Parquet в S3.
**Trade-off:** один endpoint vs два кода. Принято: единый OpenAPI с двумя режимами по `Accept`.

### ADR-005 — URL-версионирование `/v1/...` + строгий OpenAPI
**Решение:** breaking change → `/v2`, additive → бэкпорт в `/v1`. OpenAPI лежит в репо, генерируем клиент для X-Flow.

### ADR-006 — Severity-движок валидации, config-driven
**Решение:** правила в `validation_rules.yaml`, severity = critical|soft. Стартовая политика — см. §4.3.
**Trade-off:** жёсткий старт (отрицательные остатки = critical) приведёт к фейлам в первый месяц, но это лучше чем тихая порча витрин.

### ADR-006a — Lifecycle классифицируется на нашей стороне, не тянется как готовый ярлык
**Решение:** из ERP тянем сырьё (`store_assortment.target_qty`, даты листинга, ссылки на промо-механику, глобальный «архив» карточки). Ярлыки `pending / active / phasing_out / discontinued` присваиваются нашим классификатором по правилам в `lifecycle_rules.yaml`. Версия правил пишется в `store_assortment.load_id` для воспроизводимости.
**Trade-off:** дополнительный код vs независимость от того, как именно конкретный товаровед E-Zoo заполняет поле статуса. Расцепляет наши релизы с ERP. Зависит от ответа на OQ-7 (если ERP даёт явный статус — добавляем `source='erp'` режим как fallback).

### ADR-007 — Atomic restart per load_id, 2 snapshot минимум
**Решение:** при сбое — откат всего `load_id`. X-Flow всегда видит committed snapshot. Хранится минимум 2.
**Trade-off:** потенциально ре-чтение 10M строк из ERP против resume-checkpoint. Принято: проще, идемпотентно; resume — отложенная оптимизация.

### ADR-008 — PII-стратегия: hash + drop
**Решение:** `loyalty_id` → `SHA256(id || salt)`; phone/email — drop. Salt в Vault, ротация ≤ раз в год.

### ADR-009 — Retention двухуровневый
**Решение:** 30d hot PG + 365d cold S3 (Parquet, partitioned by date).
**Trade-off:** дороже код (двухуровневый sweeper), но дешевле PG-storage и быстрее backfill.

### ADR-010 — Триггер: внутренний cron + admin-API
**Решение:** Go-cron внутри процесса + `POST /admin/loads` для manual/retry. K8s CronJob НЕ используем — меньше внешних зависимостей.

---

## 8. API contract (черновик OpenAPI)

```
GET  /v1/healthz
GET  /v1/snapshots                              # список доступных snapshot
GET  /v1/snapshots/current                      # текущий committed snapshot id

GET  /v1/products?since=...&cursor=...                 # NDJSON inline; .status = active|archived
GET  /v1/product_barcodes?since=...&cursor=...
GET  /v1/category, /v1/location, /v1/supplier,
GET  /v1/store_assortment?state=active,phasing_out&location_id=... # фильтр по lifecycle_state
GET  /v1/store_assortment/lifecycle_events?since=...               # журнал переходов
GET  /v1/master_change_log?entity=...&since=...&field=...    # diff-журнал мастер-данных (см. §4.5.a)
GET  /v1/supplier_stock_snapshot?supplier_id=...&since=...   # остатки у поставщика (см. §5.2.4)
GET  /v1/supply_spec, /v1/promo,
GET  /v1/supply_plan, /v1/order_rule

POST /v1/exports          {entity, snapshot_id, format=parquet}   → {export_id}
GET  /v1/exports/{id}     → {status, download_url?, parts?}

# admin
POST /admin/loads         {force=true?}         # ручной запуск
POST /admin/loads/{id}/retry
GET  /admin/loads/{id}    # статус, errors, reject_log summary
GET  /admin/reject-log?load_id=...&entity=...
```

Все ответы включают:
- `X-Snapshot-Id` / `X-Load-Id`
- `ETag` (хеш от `snapshot_id + cursor`)
- `Cache-Control: private, max-age=86400` (snapshot immutable)

---

## 9. Observability & alerts

**Метрики (Prometheus):**
- `data_export_load_duration_seconds{entity, status}`
- `data_export_rows_total{entity, snapshot_id}`
- `data_export_rejects_total{entity, severity}`
- `data_export_source_request_duration_seconds{endpoint}`
- `data_export_snapshot_age_seconds` (gauge)

**Алерты:**
1. **Cron не стартовал** в окне +15 min → P1.
2. **Duration > 2× медианы за 14d** → P2.
3. **Reject_rate > 0.1% за load** → P2.
4. **Объём < 50% от вчера (per entity)** → P1.

---

## 10. Verification (как проверить, что работает)

1. **Unit + integration (dockertest):** PG18 контейнер, прогоняем full load на синтетическом источнике (mock REST), проверяем `loads.status='committed'`, `snapshot_pointer` сдвинулся, `reject_log` пуст для clean-input.
2. **Golden contract test:** OpenAPI v1 контракт фиксируется в репо; тест ломается при breaking change без bump версии.
3. **Severity-тесты:** 4 critical-кейса (FK miss, NULL PK, negative qty, future event) → load failed; 3 soft-кейса (NULL attr, bad format, EAN checksum) → load committed + reject_log.
4. **Atomic restart:** прерываем mock-источник на 50% → load failed → snapshot_pointer НЕ двинулся → следующий retry с нуля → committed.
5. **Read-during-write:** запускаем load, параллельно `GET /v1/products` → отдаёт previous snapshot, не видит running load.
6. **Performance smoke:** 1M receipt_line за 5 минут на dev-окружении (пропорционально 12M/час production).
7. **End-to-end в staging:** реальный 1С-mock с >100K товаром и ≥1.5 баркода на карточку (тест мульти-баркод lookup), измеряем длительность, объём rejects, размер Parquet-файлов.
8. **Multi-barcode lookup:** в моке создаём товар с 3 EAN, чек ссылается на каждый — все 3 строки разрешаются в одну `product_id`. Дубликат EAN на двух карточках → critical, load failed.

---

## 11. Open questions / risks

| # | Вопрос | Кому | Когда нужен ответ |
|---|---|---|---|
| OQ-1 | Метод аутентификации к ERP API (OAuth2 cc / mTLS / API-key + IP allowlist) | ИБ E-Zoo | до старта реализации |
| OQ-2 | Реальный объём (SKU, locations, receipt_line/сутки) | IT E-Zoo | до Performance smoke |
| OQ-3 | Какой именно ERP (1С v?, SAP, кастом) и какие endpoints доступны | IT E-Zoo | до Sprint 1 |
| OQ-4 | EDI-профиль (EDIFACT D.01B / GS1 EANCOM) — см. [diagram_review.md §3.3](../../diagram_review.md). EDI-настройки поставщиков пока не моделируем (раздел `supplier_edi` исключён); вернёмся к нему когда определится EDI-провайдер | EDI-провайдер | можно после MVP |
| OQ-5 | RECADV / РН з ЕЦП — отдельные документы или только статусы? — [diagram_review.md §3.1, §3.2](../../diagram_review.md) | бизнес E-Zoo | до фазы 4 (приёмка) |
| OQ-6 | Multi-tenant в обозримом будущем? | продукт | если «да» — пересмотреть ADR в первый же квартал |
| OQ-7 | Где в ERP лежит сигнал «распродажа по конкретному магазину» — отдельный статус в карточке ассортимента ТТ, признак промо-механики, или операторы просто обнуляют `target_qty`/MSP? От ответа зависит, читаем `lifecycle_state` напрямую или реконструируем правилами | IT E-Zoo + категорийный менеджер | до Sprint 1 (блокирует §5.1.4) |
| OQ-8 | Маркировка ветпродукции / лекарственных препаратов: есть ли в Украине обязательная система прослеживаемости (аналог «Меркурия») для ветпрепаратов, и должны ли мы выгружать признак `requires_marking` + связанные коды? Если да — нужны поля в `products` и в `stock_movement` | юрист/QA E-Zoo | до фазы 4 (приёмка) |
| OQ-9 | Каннибализация / substitutes: есть ли у E-Zoo формализованная матрица замен (для расчёта lost sales и переключения прогноза при OOS)? Если есть — в каком объекте ERP лежит | категорийный менеджер | можно после MVP |
| OQ-10 | Сегментация для зоо (animal_kind, age_segment, size_segment, feeding_type): полностью ли покрывается `category_path` или нужны отдельные теги? | категорийный менеджер | до Sprint 2 |
| OQ-11 | Источник `supplier_stock_snapshot`: какие поставщики дают видимость остатков и каким способом (EDI INVRPT / REST API дистрибьютора / Excel-выгрузка / нет). От этого зависит, какие коннекторы строить и что считать «инфинитной поставкой» по умолчанию | закупки + IT E-Zoo | до Sprint 2 (без этого таблица будет пустой для большинства поставщиков) |

**Риски:**
- ERP REST API по производительности не справится с pull `>10M receipt_line/сутки` за окно → потребуется CDC раньше плана.
- Severity-config окажется too-strict в первый месяц → готовиться к ежедневным правкам YAML.
- VPN site-to-site flapping → длительные ретраи, нужен backoff cap.

---

## Critical files (планируемые)

- `internal/features/data_export/router/router.go` — Fiber routes `/v1/...`, `/admin/...`
- `internal/features/data_export/loader/` — orchestrator + `SourceReader` interface
- `internal/features/data_export/loader/erp_e_zoo_reader.go` — реализация под REST ERP
- `internal/features/data_export/validation/` — severity engine + YAML loader
- `internal/features/data_export/storage/migrations/` — golang-migrate SQL
- `internal/features/data_export/exports/` — async exports + S3 Parquet writer
- `configs/validation_rules.yaml` — стартовая политика
- `api/openapi/v1.yaml` — контракт для X-Flow
